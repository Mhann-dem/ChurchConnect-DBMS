# 📊 Reports Feature - Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        CHURCHCONNECT REPORTS SYSTEM                      │
└─────────────────────────────────────────────────────────────────────────┘

FRONTEND ARCHITECTURE
═════════════════════════════════════════════════════════════════════════

User Interface Layer
    ┌──────────────────────────────────────────────────────────────┐
    │  /reports Page (Main)                                        │
    ├──────────────────────────────────────────────────────────────┤
    │  ┌────────────┬──────────────┬──────────────┐               │
    │  │ Statistics │   Generator  │   History    │               │
    │  │  (Tab 1)   │   (Tab 2)    │   (Tab 3)    │               │
    │  └────────────┴──────────────┴──────────────┘               │
    └──────────────────────────────────────────────────────────────┘

Component Layer
    ┌─────────────────────────────────────────────────────────────┐
    │ ReportStats              ReportGenerator                     │
    │ • 6 KPIs                 • Report type selector              │
    │ • Type breakdown         • Format selection                  │
    │ • Recent runs            • Column picker                     │
    │ • Storage info           • Filter builder                    │
    │ • Template usage         • Template quick apply              │
    │                          • Form submission                   │
    └─────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────────────────────┐
    │ ReportHistory            ReportScheduler                     │
    │ • Report list (filtered) • Scheduling toggle                 │
    │ • Run report             • Frequency selector                │
    │ • View history           • Email recipients                  │
    │ • Delete report          • Email customization               │
    │ • Bulk actions           • Configuration save                │
    └──────────────────────────────────────────────────────────────┘

API Integration Layer
    ┌────────────────────────────────────────────────────────────┐
    │ Axios Client (apiClient)                                   │
    │ • JWT authentication                                       │
    │ • Token refresh handling                                   │
    │ • Error interceptors                                       │
    │ • Request queuing                                          │
    └────────────────────────────────────────────────────────────┘


BACKEND ARCHITECTURE
═════════════════════════════════════════════════════════════════════════

Database Layer
    ┌────────────────────────────────────────────────────────────┐
    │ PostgreSQL Database                                        │
    ├────────────────────────────────────────────────────────────┤
    │ • ReportTemplate (5 system + user templates)               │
    │ • Report (user report configurations)                      │
    │ • ReportRun (execution history & files)                    │
    └────────────────────────────────────────────────────────────┘

Model Layer
    ┌────────────────────────────────────────────────────────────┐
    │ models.py (347 lines)                                      │
    ├────────────────────────────────────────────────────────────┤
    │ ReportTemplate                                             │
    │   • id (UUID)                                              │
    │   • name, description, report_type                         │
    │   • default_filters, default_columns                       │
    │   • usage tracking                                         │
    │                                                            │
    │ Report                                                     │
    │   • id (UUID)                                              │
    │   • name, description, report_type                         │
    │   • format, columns, filters                               │
    │   • scheduling (frequency, next_run)                       │
    │   • email settings                                         │
    │                                                            │
    │ ReportRun                                                  │
    │   • id (UUID)                                              │
    │   • status (pending, running, completed, failed)           │
    │   • file_path, file_size, record_count                     │
    │   • execution_time, error_message                          │
    └────────────────────────────────────────────────────────────┘

Service Layer
    ┌────────────────────────────────────────────────────────────┐
    │ services.py (830 lines)                                    │
    ├────────────────────────────────────────────────────────────┤
    │ ReportGeneratorService                                     │
    │   • _get_report_data()                                     │
    │     └─ Members, Pledges, Groups, Families, Statistics      │
    │   • generate_report()                                      │
    │     ├─ CSV generation                                      │
    │     ├─ Excel generation (openpyxl)                         │
    │     ├─ PDF generation (reportlab)                          │
    │     └─ JSON generation                                     │
    │   • generate_adhoc_report()                                │
    │   • _safe_file_creation() (temp file handling)             │
    │                                                            │
    │ ReportSchedulerService                                     │
    │   • schedule_report()                                      │
    │   • process_due_reports()                                  │
    │   • calculate_next_run()                                   │
    └────────────────────────────────────────────────────────────┘

Task Layer (Celery)
    ┌────────────────────────────────────────────────────────────┐
    │ tasks.py (310 lines) - Ready for Celery integration        │
    ├────────────────────────────────────────────────────────────┤
    │ • generate_report_async()                                  │
    │ • send_report_email()                                      │
    │ • process_scheduled_reports()                              │
    │ • cleanup_old_reports()                                    │
    └────────────────────────────────────────────────────────────┘

View Layer
    ┌────────────────────────────────────────────────────────────┐
    │ views.py (616 lines) - REST API ViewSets                   │
    ├────────────────────────────────────────────────────────────┤
    │ ReportViewSet (Full CRUD)                                  │
    │   • GET /reports/ → list (filtered)                        │
    │   • POST /reports/ → create                                │
    │   • GET /reports/{id}/ → retrieve                          │
    │   • PUT/PATCH /reports/{id}/ → update                      │
    │   • DELETE /reports/{id}/ → destroy                        │
    │   • POST /reports/{id}/run/ → run report                   │
    │   • POST /reports/generate/ → ad-hoc generation            │
    │   • GET /reports/stats/ → statistics                       │
    │   • POST /reports/bulk_action/ → bulk operations           │
    │                                                            │
    │ ReportRunViewSet (Read-only)                               │
    │   • GET /reports/runs/ → list executions                   │
    │   • GET /reports/runs/{id}/ → execution details            │
    │                                                            │
    │ ReportTemplateViewSet (Full CRUD)                          │
    │   • GET/POST /reports/templates/                           │
    │   • GET/PUT/DELETE /reports/templates/{id}/                │
    │   • POST .../use_template/ → create from template          │
    │                                                            │
    │ download_report() → file download endpoint                 │
    └────────────────────────────────────────────────────────────┘

Serializer Layer
    ┌────────────────────────────────────────────────────────────┐
    │ serializers.py (281 lines)                                 │
    ├────────────────────────────────────────────────────────────┤
    │ • ReportTemplateSerializer (with validation)               │
    │ • ReportSerializer (with calculated fields)                │
    │ • ReportRunSerializer (with file info)                     │
    │ • ReportGenerationSerializer (ad-hoc validation)           │
    │ • ReportStatsSerializer (statistics)                       │
    │ • BulkReportActionSerializer (bulk operations)             │
    └────────────────────────────────────────────────────────────┘


DATA FLOW
═════════════════════════════════════════════════════════════════════════

Report Generation Flow:
┌─────────┐     ┌─────────────────┐     ┌──────────────┐     ┌────────┐
│ User    │────>│ ReportGenerator │────>│ ReportRun    │────>│ File   │
│ Input   │     │ Service         │     │ Created      │     │ System │
└─────────┘     └─────────────────┘     └──────────────┘     └────────┘
                       │
                       ├─> Query Database
                       ├─> Format Data
                       ├─> Generate File
                       └─> Save to Storage

Scheduled Report Flow:
┌──────────────────┐     ┌────────────────────┐     ┌──────────────┐
│ Celery Beat      │────>│ Task Handler       │────>│ Email        │
│ (Scheduler)      │     │ (Process Reports)  │     │ Recipients   │
└──────────────────┘     └────────────────────┘     └──────────────┘
         │                         │
    Every 5 min            Generate & Email


SECURITY ARCHITECTURE
═════════════════════════════════════════════════════════════════════════

Authentication
    └─ JWT Token (via SimpleJWT)
       ├─ Access Token (15 min expiry)
       ├─ Refresh Token (24 hr expiry)
       └─ Token Rotation on Refresh

Authorization
    ├─ IsAuthenticated (all endpoints)
    ├─ IsOwner (user can see own reports only)
    ├─ IsStaff (staff see all reports)
    └─ Rate Limiting (10 reports/hour per user)

Data Protection
    ├─ Input Validation (email, JSON formats)
    ├─ SQL Injection Prevention (Django ORM)
    ├─ File Path Validation (prevents traversal)
    ├─ File Size Limits (100MB max)
    └─ Comprehensive Error Logging


DEPLOYMENT ARCHITECTURE
═════════════════════════════════════════════════════════════════════════

Production Deployment:
┌──────────────────────────────────────────────────────────────────┐
│ Nginx Reverse Proxy                                              │
├──────────────────────────────────────────────────────────────────┤
│ ┌────────────────────────┬───────────────────────┐              │
│ │ Frontend (React)       │ Backend (Django)      │              │
│ │ • Static files         │ • Gunicorn (3 workers)│              │
│ │ • SPA routing          │ • 60s timeout         │              │
│ │ • GZIP compression     │ • Connection pooling  │              │
│ └────────────────────────┴───────────────────────┘              │
└──────────────────────────────────────────────────────────────────┘

Background Jobs:
┌──────────────────────────────────────────────────────────────────┐
│ Celery Worker                                                    │
├──────────────────────────────────────────────────────────────────┤
│ • Async report generation                                        │
│ • Email delivery                                                 │
│ • Task retry on failure                                          │
│ • Result backend (Redis/DB)                                      │
└──────────────────────────────────────────────────────────────────┘

Periodic Tasks:
┌──────────────────────────────────────────────────────────────────┐
│ Celery Beat Scheduler                                            │
├──────────────────────────────────────────────────────────────────┤
│ • Every 5 min: Process scheduled reports                         │
│ • Every 24 hrs: Clean up old report files                        │
│ • Timezone-aware scheduling                                      │
└──────────────────────────────────────────────────────────────────┘


SYSTEM REPORT TEMPLATES
═════════════════════════════════════════════════════════════════════════

1. Active Members Export
   ├─ Type: Members
   ├─ Format: Excel
   ├─ Columns: ID, First Name, Last Name, Email, Phone, Date Joined, Status
   └─ Filters: Active members only

2. Monthly Pledges Report
   ├─ Type: Pledges
   ├─ Format: Excel
   ├─ Columns: ID, Member, Amount, Date, Status, Category
   └─ Filters: None

3. Group Directory
   ├─ Type: Groups
   ├─ Format: PDF
   ├─ Columns: ID, Name, Description, Leader, Member Count
   └─ Filters: None

4. Family Report
   ├─ Type: Families
   ├─ Format: Excel
   ├─ Columns: ID, Name, Head Member, Member Count, Created Date
   └─ Filters: None

5. Church Statistics
   ├─ Type: Statistics
   ├─ Format: PDF
   ├─ Columns: All statistics
   └─ Filters: None


METRICS & MONITORING
═════════════════════════════════════════════════════════════════════════

Real-time Statistics
    ├─ Total Reports: Count of all reports
    ├─ Active Reports: Count of enabled reports
    ├─ Scheduled Reports: Count with scheduling enabled
    ├─ Total Executions: Count of all runs
    ├─ Successful Runs: Count of completed runs
    ├─ Failed Runs: Count of failed runs
    ├─ Storage Usage: Total bytes used
    ├─ Reports by Type: Distribution chart
    └─ Recent Runs: Last 10 executions


FILE MANAGEMENT
═════════════════════════════════════════════════════════════════════════

Storage Structure:
    media/reports/
    └─ YYYYMM/
       └─ DD/
          └─ HHmmss_reportid_filename.ext

File Naming Convention:
    {timestamp}_{reportid}_{reporttype}_{format}.{ext}
    Example: 20251220_abc123_members_2024-12-20_120000.xlsx

Cleanup Policy:
    • Keep all files
    • Archive files older than 30 days (configurable)
    • Maximum 100MB per report file
    • Total storage limit per user (configurable)


INTEGRATION POINTS
═════════════════════════════════════════════════════════════════════════

Connects to:
    ├─ Members app (for member reports)
    ├─ Pledges app (for pledge reports)
    ├─ Groups app (for group reports)
    ├─ Families app (for family reports)
    ├─ Authentication module (for user tracking)
    └─ Email system (for report delivery)

Exposes to:
    ├─ Frontend (via REST API)
    ├─ Admin interface (Django admin)
    ├─ Management commands
    └─ Celery tasks


CONFIGURATION
═════════════════════════════════════════════════════════════════════════

Environment Variables:
    REPORTS_MAX_FILE_SIZE = 104857600  (100MB)
    REPORTS_GENERATION_TIMEOUT = 300   (5 min)
    REPORTS_RETENTION_DAYS = 30        (cleanup age)
    REPORTS_CACHE_TIMEOUT = 300        (5 min)

Settings:
    MEDIA_ROOT = absolute path
    MEDIA_URL = '/media/'
    DEFAULT_FROM_EMAIL = 'reports@churchconnect.org'
    EMAIL_BACKEND = 'smtp'


═════════════════════════════════════════════════════════════════════════

Total Components: 20+
Total Lines of Code: 5,000+
Total API Endpoints: 15+
Documentation Pages: 3
Time to Setup: ~10 minutes

Status: ✅ PRODUCTION READY

═════════════════════════════════════════════════════════════════════════
```

## Component Interaction Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          USER WORKFLOW                                   │
└─────────────────────────────────────────────────────────────────────────┘

1. VISIT REPORTS PAGE
   User → Browser → /reports → ReportsPage (React)
                              ↓
                         3 Tab Options
                         ├─ Statistics
                         ├─ Generator
                         └─ History

2. GENERATE REPORT
   ReportGenerator → Form Submission
                  ↓
              API Call (POST /api/v1/reports/generate/)
                  ↓
              Backend ViewSet → Service → Database
                  ↓
              File Generation (CSV/Excel/PDF/JSON)
                  ↓
              Browser Download
                  ↓
              Success Notification

3. SCHEDULE REPORT
   ReportScheduler → Configuration
                  ↓
              API Call (PATCH /api/v1/reports/{id}/)
                  ↓
              Backend Updates Report Model
                  ↓
              Celery Scheduler Picks Up
                  ↓
              Automatic Generation & Email
                  ↓
              Success Notification

4. VIEW HISTORY
   ReportHistory → List Component
               ↓
           API Call (GET /api/v1/reports/)
               ↓
           Display List
               ├─ Run Existing
               ├─ View Executions
               └─ Download Files

5. VIEW STATISTICS
   ReportStats → Metrics Dashboard
             ↓
         API Call (GET /api/v1/reports/stats/)
             ↓
         Display Charts & Metrics
             ├─ KPIs
             ├─ Type Breakdown
             └─ Recent Runs


═════════════════════════════════════════════════════════════════════════
Last Updated: December 20, 2025
Version: 1.0.0
Status: ✅ Production Ready
═════════════════════════════════════════════════════════════════════════
```
